// TODO - add error handling hooks. See https://kit.svelte.dev/docs#hooks

/*
export function handleError({ event, error: any }) {
  console.error(error.stack ?? error);

  return {
    message: "everything is fine",
    code: "500"
  };
}
*/